# 統一換行，確保 build_id（關鍵 source 檔內容 hash）跨機器可重現。
# 文字檔一律以 LF 儲存與 checkout；Windows 腳本亦使用 LF（PowerShell 可讀）。
* text=auto eol=lf

# 二進位（不轉換）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.parquet binary
*.duckdb binary
*.sqlite binary
*.pth binary
*.safetensors binary
