# ── Secrets / credentials ──
.env
.env.*
!.env.example
*.key
*.pem
*.pfx
*.crt
credentials/
secrets/

# ── Virtual environments ──
.venv/
.venv-fincast/
.venv-yuanta-futures-x86/
venv/
ENV/

# ── Python caches ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# ── Logs / reports ──
logs/
*.log
/reports/

# ── Caches ──
cache/
.cache/
.hf_cache/
huggingface_cache/
hf_cache/

# ── Model weights / checkpoints (NEVER commit) ──
models/cache/
models/checkpoints/
checkpoints/
*.pt
*.pth
*.ckpt
*.safetensors
*.onnx
*.bin

# ── Databases ──
*.sqlite
*.sqlite3
*.db
*.duckdb

# ── Market data (licensed / provider data — NEVER commit) ──
data/raw/
data/processed/
data/cache/
data/private/
data/registry/
data/tournament/
data/feature_store/
data/analysis_archive/
data/strategy/
data/events/
data/normalized/
data/features/
data/predictions/
data/manifests/
*.parquet
*.feather

# ── Experiment tracking ──
mlruns/
output/
outputs/

# ── Third-party cloned repos (download on demand, not vendored) ──
external/fincast/
external/tradingview-mcp/

# ── Yuanta vendor binaries (NEVER commit) ──
vendor/
*.dll
*.pfx
*.p12

# ── Local backups / agent scratch ──
.remediation_backup_20260919/
.opencode/
environment_export/
MARKET_AI_BACKUP/

# ── Phase 2 runtime artifacts (regenerated / machine-specific / private) ──
data/*.json
data/*.jsonl
FORWARD_SHADOW_STATUS.json
FORWARD_DAILY_SUMMARY.md
RESOURCE_GOVERNOR_STATUS.json
REAL_PROVIDER_VALIDATION.json
PERFORMANCE_AFTER.json
PERFORMANCE_BASELINE.json
LOCAL_DATA_INVENTORY.json
LOCAL_OSE_MICRO_COVERAGE.json
LOCAL_DATA_DUPLICATES.csv
YUANTA_LOCAL_SDK_INVENTORY.json
YUANTA_FUTURES_AUTH_RESULT.json
YUANTA_REAL_AUTH_RESULT.json
YUANTA_FUTURES_COM_DIAGNOSTIC.json
YUANTA_SPARK_QUOTE_PROBE_RESULT.json
YUANTA_INTEROP_DIAGNOSTIC.json
MICRO_HISTORY_COVERAGE_REPORT.md
YUANTA_COMPONENT_INVENTORY.md

# ── IDE / OS ──
.idea/
.vscode/
*.swp
Thumbs.db
desktop.ini

# Experiment tracking
lightning_logs/
mlruns/

# Phase 2Q-A benchmark output
MCP_FASTPATH_BASELINE.json

# Phase 2Q-B E2E output
MCP_E2E_STABILITY.json

# runtime backups (local-only)
data/backups/
data/db/
data/logs/
data/research_outputs/
data/diagnostics/
data/validation_samples/
